hydra:
  sweep:
    dir: ${results_location:}
  output_subdir: conf
  job_logging:
    formatters:
      simple:
        format: '[%(asctime)s %(levelname)s] %(message)s'
    handlers:
      file:
        encoding: utf-8
